export component PaintCanvas inherits TouchArea {
  in-out property <image> canvas <=> canvas.source;
  in-out property <image> canvas_preview <=> canvas_preview.source;

  canvas := Image { }
  canvas_preview := Image { }
}
